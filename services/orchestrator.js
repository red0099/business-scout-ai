import { GoogleGenAI } from "@google/genai"; const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY }); export async function executePipeline(url) { console.log("🚀 Orchestrator analyzing:", url); const prompt = `أنت محلل أعمال خبير. حلل هذا الموقع: ${url}. أعد تقريراً مفصلاً يشمل: 1. تحليل السوق (Market), 2. تحليل المنافسين (Competitor), 3. الوضع المالي (Financial), 4. المخاطر (Risk), 5. استراتيجية التسويق (Marketing), 6. الجوانب القانونية (Legal), 7. خيارات التمويل (Funding), 8. تقرير ختامي (Report). أجب بصيغة JSON.`; try { const response = await ai.models.generateContent({ model: "gemini-2.0-flash", contents: prompt }); const text = response.text; const clean = text.replace(/```json/g, "").replace(/```/g, ""); const results = JSON.parse(clean); return results; } catch (error) { console.error("Gemini error:", error.message); return { error: "فشل الاتصال ب Gemini", details: error.message }; } }
